name: Angular CI Workflow Template

on:
  workflow_call:
    inputs:
      runs-on:
        required: false
        type: string
        default: "ubuntu-latest"
      bun-version:
        description: "Bun version to use"
        required: false
        type: string
        default: "latest"
      working-directory:
        description: "Directory containing Angular projects"
        required: false
        type: string
        default: "./gui"
      commands:
        description: 'List of bun commands to run. These come from the "scripts" section in your project''s package.json.'
        required: false
        type: string
        default: '["build", "lint"]'

permissions: {}

jobs:
  angular:
    name: Run Angular commands
    runs-on: ${{ inputs.runs-on }}
    permissions:
      contents: read
    steps:
      - name: Check out code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Find Angular projects
        id: find-projects
        working-directory: ${{ inputs.working-directory }}
        run: |
          if [ -f angular.json ]; then
            # Extract project names from angular.json
            projects=$(jq -r '.projects | keys | @json' angular.json)
            echo "projects=$projects" >> $GITHUB_OUTPUT
            echo "Found Angular projects: $projects"
          else
            echo "projects=[]" >> $GITHUB_OUTPUT
            echo "No angular.json found"
          fi

      - name: Install Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: ${{ inputs.bun-version }}

      - name: Install dependencies
        working-directory: ${{ inputs.working-directory }}
        run: bun install

      - name: Run commands for each project
        working-directory: ${{ inputs.working-directory }}
        run: |
          projects='${{ steps.find-projects.outputs.projects }}'
          commands='${{ inputs.commands }}'
          echo "Running commands for projects: $projects"
          echo "Commands to run: $commands"

          for project in $(echo $projects | jq -r '.[]'); do
            for command in $(echo $commands | jq -r '.[]'); do
              echo "::group::Running $command for $project"
              bun run $command $project || bun run $command
              echo "::endgroup::"
            done
          done
